function chage(index) {
    const header = document.getElementById('header');
    console.log('header', header);
    switch (index) {
        case 1:
            header.textContent="C語言-河內塔"
            break;
    }
}